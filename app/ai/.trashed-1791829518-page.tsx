"use client";
import {useState} from "react";
export default function AI(){const[q,setQ]=useState("");const[a,setA]=useState("");return <main className="page"><div className="container"><h1>🤖 AfriBoost AI</h1><p className="muted">Ton assistant pour apprendre, travailler, entreprendre et trouver des opportunités.</p><form className="search" onSubmit={e=>{e.preventDefault();setA(`Pour « ${q} », AfriBoost peut construire un parcours personnalisé : comprendre ton objectif → trouver des ressources → améliorer ton profil → identifier des opportunités.`)}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Pose ta question..." /><button className="btn btn-green">Demander</button></form>{a&&<div className="card" style={{marginTop:25}}><h3>Réponse AfriBoost AI</h3><p>{a}</p></div>}<div className="wide-ai" style={{marginTop:25}}><div><h2>Transforme tes questions en actions.</h2><p>Recherche, apprentissage, CV, emploi et business dans un même assistant.</p></div><div style={{fontSize:70}}>✨</div></div></div></main>}
